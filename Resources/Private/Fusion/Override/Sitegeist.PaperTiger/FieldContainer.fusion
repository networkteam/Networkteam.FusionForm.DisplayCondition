prototype(Sitegeist.PaperTiger:FieldContainer) {

    renderer {

        default {
            // Set displayCondition property on Neos.Fusion.Form:FieldContainer
            renderer {
                displayCondition = ${q(node).property('displayCondition')}
            }
        }
    }
}
